-- ============================================================
-- task_node 表：node_id 列更名为 node_type
--   节点在链内的业务身份是"节点类型"（业务侧用枚举 name 传入，同一任务链内唯一），
--   节点本身没有业务 ID——ID 只是数据库主键/行标识组件，无业务含义。
--   原 node_id 列存的就是节点类型值，这里只做更名与注释修正，数据不变。
-- ============================================================

ALTER TABLE task_node CHANGE COLUMN node_id node_type VARCHAR(64) NOT NULL COMMENT '节点类型（业务侧枚举 name，同一任务链内唯一）';
