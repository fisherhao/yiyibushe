<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- AI 凭证表 ai_credential -->
<mapper namespace="com.dayu.yiyibushe.dao.mybatis.CredentialMybatisMapper">

    <sql id="selectColumns">
        id, credential_id, provider, app_key, app_secret, status, remark, gmt_create, gmt_modify
    </sql>

    <select id="selectByProvider" resultType="com.dayu.yiyibushe.dao.po.CredentialPO">
        SELECT <include refid="selectColumns"/>
        FROM ai_credential
        WHERE provider = #{provider}
    </select>

    <select id="selectAll" resultType="com.dayu.yiyibushe.dao.po.CredentialPO">
        SELECT <include refid="selectColumns"/>
        FROM ai_credential
        ORDER BY credential_id
    </select>

    <insert id="insert">
        INSERT INTO ai_credential (credential_id, provider, app_key, app_secret, status, remark)
        VALUES (#{credentialId}, #{provider}, #{appKey}, #{appSecret}, #{status}, #{remark})
    </insert>

    <update id="updateByProvider">
        UPDATE ai_credential
        SET app_key    = #{appKey},
            app_secret = #{appSecret},
            status     = #{status},
            remark     = #{remark}
        WHERE provider = #{provider}
    </update>

    <delete id="deleteByProvider">
        DELETE FROM ai_credential
        WHERE provider = #{provider}
    </delete>

</mapper>
